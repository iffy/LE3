STEP export (#65) and import (#71) via OCCT: with --features occt, single-body STEP export writes real BREP (planar + curved surfaces) via STEPControl_Writer and import reads real BREP incl. curved/NURBS surfaces via STEPControl_Reader (tessellated to a body). The hand-rolled faceted-BREP step.rs path remains the no-kernel fallback (and for whole-document/multi-body export).
