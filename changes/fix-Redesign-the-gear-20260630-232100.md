Redesign the gear, chamfer/fillet, and shading-mode icons for clarity: the gear is now a real cog silhouette instead of sun-ray spokes, chamfer/fillet show a bigger bolder corner treatment with depth shading, solid+wireframe uses true negative-space cutouts instead of a faint dashed overlay, and transparent-solid's opacity is bumped to actually read as filled next to plain wireframe.
