Let a bezier curve handle be selected (click) and removed via Delete/Backspace or a right-click 'Delete handle' menu item, straightening the line — no separate drag was previously the only way to undo an accidental curve.
